spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: user
    password: password

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

  data:
    redis:
      port: 6379
      host: localhost
      channel:
          event: "event_channel"
          goal: "goal_channel"

  kafka:
    bootstrap-servers: localhost:9092
    consumer:
      group-id: rating-service
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer

kafka:
  topics:
    user-actions: user-actions-topic

server:
  port: 8080

logging:
  level:
    root: info
    school.faang.user_service.client.PaymentServiceClient: DEBUG
    feign: DEBUG

cloud:
  aws:
    s3:
      bucket: faang-user-avatars
      region: ru-central1
      endpoint: https://storage.yandexcloud.net
      access-key: YCAJEjOnrEnDD1dd6k0Urz0uV
      secret-key: YCPlcNXzXhdJze02_59Z60QlrExzj8Q2p37ZM6gO

project-service:
  host: localhost
  port: 8082

payment-service:
  host: http://localhost:9090/api

user:
  password:
    min:
      length: 6

skill:
  count-recommendation:
    min-length: 3
    
telegram:
  bot-username: BotGetChatId
  bot-token: 8237783798:AAHRORzy1IaPVJwY6eNQgFhuTYTLLDiOslY

event:
  delete-scheduled:
    cron: "0 0 0 * * *"

event-bucket:
  count: 100

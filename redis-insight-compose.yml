version: '3'
services:
  redis-insight:
    image: redislabs/redisinsight:latest

    ports:
      - "5540:5540"
    environment:
      - REDIS_HOST=host.docker.internal
      - REDIS_PORT=6379
    restart: unless-stopped